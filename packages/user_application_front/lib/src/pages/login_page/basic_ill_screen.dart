import 'package:flutter/material.dart';

class BasicIllScreen extends StatefulWidget {
  @override
  _BasicIllScreen createState() => _BasicIllScreen();
}

class _BasicIllScreen extends State<BasicIllScreen> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('질병기초정보'),
        ),
        body: Center(
          child: Column(
            children: [],
          ),
        ));
  }
}
